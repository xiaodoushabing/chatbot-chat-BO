import { PageHeader } from '../../components/ui/display';

export default function ProjectSettings() {
  return <PageHeader title="ProjectSettings" sub="Placeholder — built by the page workflow." />;
}
